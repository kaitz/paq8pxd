v140
    -In tiff parser enable lzw type.
    -In tga parser detect 8bit RLE compressed images.
    -In codec add 24/32bit image filter for LZW compressed images.
    -Cleanup codec statistics.
    -In some cases us pdflzw parser as image detector.
    -Fix exe parser for small input lenghts.
    -Fix deca parser and fix for small input lenghts.
    -In gif parser change min input size to 32 bytes.
    -Add .so extension to virtual exe group.
    -Allow deca parser only in virtual exe group.
    -Add .js and .css in to text parser group.
v139
    -In tga parser wait for data end.
    -In codec test 24/32 bit image pre-post transfom size.
    -Fix tiff parser, bad detection, crash.
    -Fix SZDD parser, incorect lenght when recompressing.
    -In codec add fail statistics. Show summary.
v138
    -Add zip reduce filter, not active.
    -Fix shrink filter.
    -Add zip implode filter, not active.
    -Move data statistic in to Codec.
    -Cosmetic change to type info.    
    -Fix silent transform fails, decompress not identical.
    -Fixes in SZDD parser.
    -In tiff parser ignore image if compressed and tag 279 is missing.
    -In pdflzw parser add FlateDecode detection only for non-image data
     and do some fixes.
v137
    -Show count of files in tar parser.
    -Fix iso parser for large files (+4G).
    -Fix mdf parser.
    -Fix cd parser.
    -Cleanup compare/extract log. Show final summary.
    -Add shrink/unshrink filter for .zip
    -Fix tiff parser.
    -Fix zip parser.
v136
    -Add InstallShield CAB parser (ver 5), contains zlib streams.
     Active for extension .cab
    -Add virutal file group for .cab extension
    -In PDFLzwParser add JPXDecode and JBIG2Decode as CMP type.
    -Re-enable .zip based extension parser
    -Add .tga based extension parser
    -In zip parser extract file extension and allow uncompressed files
    -In szddparser set min lenght to 128 bytes.
    -Fix bad parsing in mrbparser.
v135
    -In zlib parser set priority to 3 if possible image header was detected.
    -In iso parser show info about how many files are there.
v134
    -In png parser detect iCCP compressed chunk, test for grayscale
    -Add iso 9960 parser. Active for extension .iso
    -Cleanup and small fixes
    -Fix pdfbiparser, set min width limit to >1
    -Disable pre- and post-filter (WRT) for streams 8 and 9.
     In rare cases, the result will not be identical.
    -In the ISO parser, make sure there are no previous folder entries.
v133
    -In mdf parser set minimum length to 4 sectors
    -Change text parser
    -Reset info text in zlib parser
    -Fix wrong info when MT is not defined
    -Remove some text related code from codec
    -No parser outputs BINTEXT, ISOTEXT so disable in most places
v132
    -Disable zip extension. If files are stored, the contents will not be recognized.
    -Limit gzip min size to 6.
    -Fixes in codec.
    -Use a maximum of one temporary memory/disk file per recursion level.
     In some exceptional cases, more. To reduce memory relocation.
    -Fix tar parser
    -Show less parsing progress
    -Fix png compression
    -Detect tar without ustar
    -Cleanup
    -More file extension based parser selections
v131
    -Add file extension based parser selection and virtual files
    -Enable tar recursion mode, select parser for each file from file extension
v130
    -Fix large memory leaks in parser, now mem usage ~400MB RAM, before in GB
    -Add tar parser recursive mode (disabled)
    -Fix gzip parser, small files not detected
    -bzip2 parser failed to detect small blocks
    -zlib parser priority change and fix for bad detection
    -Changes to textparser (needs rewrite)
v129
    -Fix bzip2, tiff parser
    -Add 0 priority parser changes to:
     base64(1/2), bmp, pnm, szdd.
     Priority changes from n to 0 when valid header is found and length of data is >64000
    -Change zlib priority
    -Re-enable all parser if 0 parser reports fail.
v128
    -Change priority to 0 in parsers:
     bzip2 - after 4 blocks
     cd - after 32 sectors
     mdf - after 32 sectors
     tar - after 4 files
v127
    -Expand priority range to 0-6, 6=default
    -Fix tar, bzip2, text parser
    -Add support for files larger then 2GB
    -In text parser switch to 0 priority when at least 32MB is processed (parsing from ~6mb/s to ~40mb/s)
v126
    -Fix cd, szdd parser
    -Display correct image info in pngparser
v125
    -Fix mdf detection
    -Fix codec reference rebound issue
    -Re-enable EOLTEXT
v124
    -Fix detection
v123
    -Fix gzip parsing.
    -Fix bzip2 memory leak.
v122
    -Re-enable zlibFilter.
    -Add PREFLATE type.
    -Test ZLIB transform fail and use PREFLATE if needed.
    -Fix for bzip2, tar parser.
v121
    -Fix GZIP parser false positive detection that suppressed bzip2/tar detection.
     The fallback was incorrectly returning END even when preflate validation failed.
v120
    -Reintroduce PNG (.png) file format detection and preprocessing support.
    -Understands multi-IDAT chunks.
    -Non-standard and optimized images are recognized as well.
v119
    -Add GZIP (.gz) file format detection and preprocessing support.
    -Implement GZIP header detection with proper header length calculation.
    -Integrate with deflate preprocessing filter for compression/decompression.
v118
    -Add ZIP file format detection and deflate compression handling.
    -Enhance analyzer to detect ZIP files and handle their deflate streams.
v117
    -Replace zlib filter with preflate filter for improved compression.
    -Add build support for the preflate library.
    -Fix non-portable GCC-only compiler flags.
    -Correct C++ standard header includes.
    -Fix case-sensitivity in predictor includes.
    -Fix jpeg detection. 
     Tiff can be as Exif image inside jpeg, so set same priority.
v116
    -Fix zlib, mdf, exe parser.
    -Update wav parser.
    -Add mscf (MS Cab) parser.
    -Change Analyzer.
v115
    -Add tar, png, bzip2, szdd parser.
    -Tiff parser:
     Detect more jpg data.
     Sort image data offsets in to ascending order.
     Enable some types and tags.
     Fix MM mode detection.
     If tag data pos is past current pos then ignore image.
     Uncompressed data with more then 32bits are reported as DEFAULT.
    -Fixes for szdd, bmp, gif, tiff, jpeg, uue, cd, mdf, bzip2 parser.
    -Update text parser.
    -In parser add last block flag.
    -Add parser info string (width, etc).
    -Add parser/codec progress status.
    -Change Analyzer
v114
    -Add pdf lzw, gif, dBase, pdf bitmap, aiff, ascii85,
     base64 (stream), base64 (multiline), mod, sgi/rgb,
     tga, cd, mdf, uue, tiff parser.
    -Some cleanup
    -Change Analyzer and Codec so that parent type is passed on in recursion.
    -Move LZSS to separate header
    -Disable zlib parser if parent type is ZLIB
    -Enable pdf bitmap parser only if parent type is ZLIB
v113
    -Update build script
    -Add default, bmp, text (very agressive), DEC Alpha, mrb (.hlp),
     exe, zlib, nes rom, jpeg, wav, pnm and MS-Zip (.cab) parsers.
    -Add analyzer (detect replacement)
    -Heavy cleanup of code
    -Enable MRBR4, TXTUTF8 and BIGTEXT filters
v112
    -More refactoring of source code
v111
    -Refactored source code
    -Add build script for Windows
v110
    -In BZip2 recompression add a new parameter to BZ2_bzCompressInit API.
     In BZip2 v1.0.3 maxLen was changed from 20 to 17. Test 20 if 17 fails.
     This is needed for older archives or recompression fails.
    -Revert minimum dictionary frequency to 19.
    -Some cleanup.
    -Change tempFile memory from 16MB to 64MB
    -Add english.dict from fxcm
v109
    -If minimum dictionary frequency=0 then use only static dictionary (-e0,dictionary)
    -Add verbose levels 0-3.
    -Remove some text colors.
v108
    -Merge VLI fix.
    -Fix BZ detection https://encode.su/threads/1464-Paq8pxd-dict?p=83831&viewfull=1#post83831
v107
    -Fix detection and compression on some jpeg files.
v106
    -Change mixer, so that there is minimum limit for update.
    -Add more StationaryMap contexts to jpegModel
    -Use larger update limit for some mixer context in jpegModel
    -Update lstm to v3
v105
    -Fix CD detection.
    -Fix MRB detection.
v104
    -in textModel also use regular word in stemmer
    -in wordModel, textModel use upper char flag from wrt 
    -DEC Alpha block-end fix from paq8px_v202
v103
    -in textModel use decoded word in stemmer to get real word type
v102
    -change ContextMap
    -adjust jpegModel mixer update
    -one context in WordModel
    -change indirectModel
        - split context into individual ContextMaps
    -add one mixer input in nestModel
v101
    -lstm +1 apm
    -in ContextMap2 add another input from run length
    -in ContextMap2 allow to select individual inputs for mixer
    -change option -e:
        -e0 - on text file do only capital letter conversion and escapes (file will be larger)
        -eX - where X > 0 use as minimum dynamic word frequency.
              -e19 recomended for files >1MB, -e124 for <1MB
              if wrt file is larger from source then transform is ignored
        -eX,dictionary - X selects frequency for words in dictionary file,
                         dynamic word frequency is fixed (-e19 files >1MB, -e124  <1MB)
        -e  - defaults to -e19
    -change wrt
    -remove model creation from predictors and add to main base predictor class
    -change exe, im24, indirect models
    -some cleanup
v100
    -add lstm model back (active on -x option)
        -used on all predictors exept audio
    -add matchModel from paq8px_v201 as second model
    -adjust old matchModel parameters
    -tar header as bintext
    -add back 1 mixer context if DECA
    -in sparsemodel (default) add 2 contexts
    -adjust normalModel
v99
    -xml/html like content processed separately in wordmodel
    -adjust some wordmodel parameters
    -attempt to detect ISO latin text
    -some changes in detection (cleanup)
v98
    -change BZip2 detection, test transform correctly
    -if in tar output filename
v97
    -fix tar detection (BZip2 to agressive)
v96
    -add some context from paq8pxd_v200 sparse model
    -adjust DEC Alpha predictor/model
    -do BZip2 recompression
v95
    -jpeg model: 
         -more context in Map1  (20)
         -more inputs from main context
         -2 main mixer inputs + 1 apm
         -cleanup
v94
    -change jpeg model
    -fix/change detection (tiff,text)
v93
    -change jpeg model
v92
    - small fixes
    - small adjustments
    - detect pdf CMYK and force as IMAGE32
v91
    - fix jpeg compression on larger levels
    - fix SZDD recompression (fail if error)
v90
    - Add DEC Alpha model/detect from paq8px193fix2
    - Small changes in mixer, statemap, apm, indirectmap, contextmap2
    - Change matchmodel, recordmodel, jpegmodel
    - Fix jpegmodel on thumnail images (or full image fails to compress)
    - Adjust predictors
v89
    - fix subsamp reset on every new jpeg 
v88
    - a bit faster jpeg compression
    - add +1 context to jpeg and change some :)
v87
    -fix decompression command line (only archive name needed again)
    -limit mem on some models
    -small changes
v86
    - change jpeg
    - -w option - +9 html entities
                - reorder: albums, clubs, disorders
                - List type articles moved to ':' section
    - wrt most upper chars converted to lower, if -w also process combined words
    - wordmodel some changes
v85
    - fix rounding error
v84
    - adjust jpeg,im1 predictor
    - add mod_SSE to all predictors exept jpeg
    - add external dictionary command line option
    - add RC to encoder (paq8pxd82sa)
    - change ppmd mem
    - in wordmodel process math tag (wiki)
v83
    - add mod_SSE to text stream predictor
    - -w option - reorder content by title beginning with number larger then 9
                - add &" to "  
v82 
    - -w option - reorder content by title selecting ',' or ':'
v81
    - fix str number detection &#xxxx;
    - wit -> more escapes for lang

v80
    - Small changes in wordModel
    - Add second option -w for direct input of wikipedia dumps
    - WIT data type (option -w) for wikipedia 
        no detection, malformed input gets transform fail
     -> subtract ID, convert timestamp, convert html entities (also to UTF8)
      | extract article header ns/id -> contributor, place after data
      | extract langs at the end of article, place after data
    - Move online wrt out from wordmodel
    
v79
    - Change wordModel1
       some html entities rollback
    - Some fixes
v78
    - Change wordModel1,recordmodel
v77
    - Change TextModel (wrt)
v76
    - Change wordModel1 to compress pdf text (from paq8px_183fix1)
    - Fix jpeg thumbnail compression
    - Make online wrt work
    - In wrt split num/utf8 chars, also some other utf8 chars. Large file mode
    - Allow large text block detection (+2GB)
    - Set utf8 for text if found
    - Change wordModel1, recordmodel to use wrt column mode
    - Change sparsemodelx
    - Small fixes
    - Show progress when detecting data
v75
    - Change wordModel1
v74
    - fix -x option on level 10-15
v73
    - Change text detection
    - Change wordModel1
    - Add mod_ppmd/chart model back (-x option)

v72
    - Change StationaryMap, StateMap
    - Change jpegModel
    - Change wordModel1
    - remove wavModel
    - added audio8, audio16

v71
    - small change in im8 compression
    - reduce compression loss in v70
v70
    - Change StationaryMap
    - Change im24 model, compresses faster.
    - detect tiff change
v69
    -Online wrt is present and fixed, now actually decodes word if code
      is larger then 1 byte.
    -WordModel form paq8px_v182
      this one is not active in any models, you have to manually add it
      in source. Replaced in model array with blankmodel for now.
      Online wrt is needed so one can detect presence of BT and ET 
      for pdf mode in new WordModel.
    -Combined models into arrays to ease mixer inputs/sets/context clutter,
      let models to return usage.
    -Colored console (Windows only!), based on type stream.
    -JPEG compress improvment (APM), small gain.
    -Detect LZW in PDF and ad to CMP stream.

v68
    -update im8Mode,im24model,exemodel from paq8px_179
    -compressed stream /MZIP,EOL,ZLIB fail/
    -linearPredictionModel from paq8px_179
    -add charModel into wordModel paq8px_179

v67
    -revert ContextMap mix
    -change wordModel
    -in bypass update models
    -change distanceModel, indirectModel, exeModel
    -in default predictor resume bypass after 0x2000 bytes if predict miss found
     insted default bypass match length of 0xfff bytes
    -compressed data stream, still compressed but faster (eol data for now)
    -memory usage tweak to the same level as v64

v66
    -some arch compiler fixes for mmx,ssse3,avx2
    -contextMap SIMD change, more speed :D
    -matchModel bypass in all streams
    -add MJPEG context in jpegModel, use min 256 byte match for bypass
    -change BHMap in jpegModel, now does actual compression
    -min bypass in image8/24 models is width
    -in textstream add another mixer context from wordModel
    -dmcModel min mem fix
    -change APM in EAPM p2

v65
    -SIMD change for a bit more speed, add also to ContextMap2
    -drop ppm and chartmodel

v64
    -jpeg model fix

v63
    -jpeg model +1 context, use bhmap
    -change wordmodel, textmodel

v62
    -bugfix

v61
    -recordmodel change from paq8px_v175
    -change recordmodel, wordmodel and textmodel
    -tga rle,tif lzw recompression from paq8px_v177

v60
    -indirect,record,sparesematch,textmodel model change from paq8px_v173
    -change chartmodel
    -uuencode

v59
    -8 and 24 bit image model,matchModel, SparseMatchModel,recordModel
     changes from paq8px_v166
    -AVX2 mixer fix
    -chartModel memorys error fix
    -fix decModel

v58
    -change PredictorJpeg
    -add chartModel from paq8k

v57
    -fix memory errors

v56
    -fix textModel mixer inputs

v55
    -revert distance model back
    -detect 4 bit mrb images, recompress only first image
    -add SparseMatchModel from paq8px_162, use on default/text/exe stream
    -add array class from paq8px_162
    -matchModel changes form paq8px_162
    -use text detection form paq8px_162, change to detext EOLTEXT,TEXT0,TEXTUTF8
    -contextMap2 form paq8px_162, use in normalModel, textModel,exeModel
    -use textModel, xmlModel in PredictorEXE
    -fix jpeg detection
    -in wrt try to find utf8 char if word not found in dict
    -in wrt change minimum words needed to 10 in final dict
    -tweak ARM, DEC, etc detection
    -brute force tar detection (non ustar)
    -other small changes and small changes from paq8px_162

v54
    -model changes
    -add back dmc model from v48

v53
    -small fixes

v52
    -change default and text predictor

v51
    -better detection for NES
    -changes from paq8px_v155
    -Estonian stemmer (minimal), mostly conflicts with French
    -add compiler info for gcc, clang
    -removed dmcModel from jpegModel
    -detect ARM executables, do address translation

v50
    -small fixes
    -add back SIMD implemetation of ContextMap byterun

v49
    -im24 model changes from paq8px_v146
    -revert to old dmcModel
    -use adaptive mixer for IMAGE24

v48
    -model changes from paq8px_v145
    -minor changes

v47
    -UNIX fix (vnx)
    -TextModel changes from paq8px_v140
    -TextModel change
    -DEC stream, model (minor gain)
    -TIFF detect changes

v46
    -AVX dotproduct change
    -UNIX includes change (vnx)
    -new TextModel from paq8px_v138
    -minor changes

v45
    -revert tar header to  HDR

v44
    -tar header as TEXT
    -wordmodel change
    -avoid setting useless context in xml and word model (set 0)
    -indirect and sparse model context hashed
    -other small changes

v43
    -change DEC Alpha detection
    -change tar detection
    -im8/im24 changes, fixes from paq8px_v129
    -wordmodel change

v42
    -small tweak

v41
    -file class, image model, wordmodel changes from paq8px_c128
    -removed option -q -f
    -small gain on heavily segmented files.
    -tweak tar parsing, fixes samba compression
    -another change to wordmodel
    -tweak jpeg class

v40
    -resolve conflict in WRT vs EOLencode
    -modify wordmodel
