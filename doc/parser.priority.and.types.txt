Parser      Priority  Types (sub-types)
bmp             3     IMAGE1 IMAGE8 IMAGE4 IMAGE24
deca            3     DECA
default         6     DEFAULT
exe             3     EXE
jpeg            2     JPEG
mrb             3     MRBR(IMAGE8,CMP), MRBR4(IMAGE4,CMP), IMAGE1
mszip           3     MSZIP(CMP)
nes             3     NESROM DEFAULT)
text            4     TEXT
wav             3     AUDIO
zlib            2     ZLIB(CMP) (in pdf IMAGE8GRAY,IMAGE8,IMAGE24,IMAGE32,IMAGE4,IMAGE1) brute=false
zlib            5     ZLIB(CMP) (in pdf IMAGE8GRAY,IMAGE8,IMAGE24,IMAGE32,IMAGE4,IMAGE1) brute=true
pnmp            3     IMAGE1 IMAGE8GRAY IMAGE24 IMAGE32
pdflzw          3     CMP
gif             3     GIF (CMP)
dbase           3     DBASE
pdfbi           3     IMAGE1 IMAGE8
aiff            3     AUDIO
ascii85         3     BASE85
base641         3     BASE64
base642         3     BASE64
mod             3     AUDIO
sgi             3     IMAGE8
tga             3     IMAGE8GRAY,IMAGE8,IMAGE24,IMAGE32,LZW
cd              1     CD
mdf             1     MDF
uue             3     UUENC
tiff            2     IMAGE1,IMAGE4,IMAGE8,IMAGE24,IMAGE32,LZW,CMP,JPEG,ZLIB,DEFAULT
tar             2     TAR,RECE
png             2     ZLIB(PNG8GRAY,PNG8,PNG24,PNG32)
bzip2           2     BZIP2(CMP)
szdd            3     SZDD(CMP)
mscf            3     CMP
zip             2     ZIP,CMP,RECE
gzip            2     GZIP
iso             2     RECE
isc             2     ZLIB

Priority  - is set by parser and type is returned by parser.
Type - is what the parser reports to "analyzer" if something was found.
Sub-type - is passed on by info parameter or desided by "codec" when transform fails or not.

There are 5 levels of priority.

0 - root parser (any below, only one can be set to 0)
1 - known container format (cd, mdf, ...)
2 - recursive types (tiff, gif, tar, ...)
3 - regular type (images, zlib ...)
4 - same as abowe with lower priority
5 - hard to parse type (text, ...)
6 - default (unknown type)

If some parser sets its priority to 0 then all other parsers are disabled, exept default parser (priority 6).